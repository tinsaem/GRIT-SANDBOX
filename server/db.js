// db.js is kept for reference only — the app now uses Prisma.
// All routes import from ./prisma/client instead.
// Run: cd server && npx prisma migrate dev --name init
module.exports = require("./prisma/client");
